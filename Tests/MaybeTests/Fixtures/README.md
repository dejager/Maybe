# Compatibility fixtures

Original synthetic programs and deterministic provider responses are generated by
`scripts/generate-oracle-fixtures.ts` against a separately downloaded Probably 0.1
interpreter. No upstream source or hosted recordings are redistributed here.
